/**
 * Connectors: per-tenant connections to outside systems with encrypted
 * credentials, the canonical inbound message every mailbox provider produces,
 * and the signing scheme of the raw-MIME ingestion endpoint (ADR-0013).
 */
export * from './domain/index.js';
