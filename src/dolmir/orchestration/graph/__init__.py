"""Reasoning Graph executor: typed nodes, Result-returning runs (Phase 2).

Rule (Standing Rule 3): data flows between nodes via typed returns and the
GraphContext — never via the Event Bus.
"""

__all__: list[str] = []
